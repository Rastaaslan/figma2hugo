"""Noms de composants semantiques compris par le rendu et la couche interactive."""

from __future__ import annotations

FORM_CONTROL_COMPONENTS = frozenset({"field", "select", "textarea", "submit"})
